import 'package:flutter/material.dart';

class TomaAsistencia extends StatelessWidget {
  const TomaAsistencia({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("toma de asistencias");
  }
}